@model DhavalTailor_Wines.Models.Wine_Type

@{
    ViewData["Title"] = "Edit a Wine Type";
}

<h1>Edit a Wine Type</h1>

<h4>Wine_Type</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ID" />
            <div class="form-group">
                <label asp-for="WineTypeName" class="control-label"></label>
                <input asp-for="WineTypeName" class="form-control" />
                <span asp-validation-for="WineTypeName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
